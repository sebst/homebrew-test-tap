name: Test Arm
on:
    workflow_dispatch:

jobs:
  test-bot:
    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04, self-hosted]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Hello World
      run: |
        echo "Hello World"
        head -n 1 /etc/os-release
        uname -a
    # - name: test qemu
    #   uses: pguyot/arm-runner-action@v2
    #   with:
    #     commands: |
    #       echo "Hello World"
    #       head -n 1 /etc/os-release
    #       uname -a
